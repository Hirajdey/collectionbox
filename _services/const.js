import { API } from '../config';

const USER_REGISTER = API + "/register";
const USER_ACTIVATION = API + "/register/activate";

const APIS = {
    USER_REGISTER,
    USER_ACTIVATION
}

export default APIS;


